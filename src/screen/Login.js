import React from "react";
import { Container, TextCustom } from "./Styled";

const Login = () => {
	return (
		<Container>
			<TextCustom>Login</TextCustom>
		</Container>
	);
};

export default Login;
